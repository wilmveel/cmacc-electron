# stand alone version of cmacc

##install
clone project
run `npm i` from command line.

## start
`npm start`

## builds
there are currently working appveyor (windows exe) and travis (macos dmg) build scripts included